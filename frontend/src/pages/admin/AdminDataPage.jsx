import React,{useEffect,useState} from 'react';import api from '../../utils/axios';import DataTable from '../../components/common/DataTable';import Pagination from '../../components/common/Pagination';import StatusBadge from '../../components/common/StatusBadge';import {useToast} from '../../components/common/Toast';
export default function AdminDataPage({type}){const [rows,setRows]=useState([]),[page,setPage]=useState(0),[pages,setPages]=useState(1),[loading,setLoading]=useState(true);const {addToast}=useToast();useEffect(()=>{setLoading(true);api.get(`/admin/${type}?page=${page}&size=20`).then(r=>{setRows(r.data.content||[]);setPages(r.data.totalPages||1)}).catch(e=>addToast(e.response?.data?.message||`Failed to load ${type}`,'error')).finally(()=>setLoading(false))},[type,page]);const configs={transfers:[{key:'studentName',label:'Student'},{key:'studentRollNumber',label:'Roll Number'},{key:'currentBusNumber',label:'Current Bus'},{key:'requestedBusNumber',label:'Requested Bus'},{key:'oldInchargeName',label:'Old In-Charge'},{key:'newInchargeName',label:'New In-Charge'},{key:'status',label:'State',render:r=><StatusBadge status={r.status}/>}],payments:[{key:'studentName',label:'Student'},{key:'rollNumber',label:'Roll Number'},{key:'orderId',label:'Order ID'},{key:'amount',label:'Amount',render:r=>`₹${r.amount||0}`},{key:'status',label:'Verified Status',render:r=><StatusBadge status={r.status}/>},{key:'paidAt',label:'Paid At'}],passes:[{key:'studentName',label:'Student'},{key:'rollNumber',label:'Roll Number'},{key:'passNumber',label:'Pass ID'},{key:'busNumber',label:'Bus'},{key:'boardingPoint',label:'Boarding Point'},{key:'validUntil',label:'Valid Until'},{key:'status',label:'Status',render:r=><StatusBadge status={r.status}/>}],notifications:[{key:'title',label:'Title'},{key:'recipientRollNumber',label:'Recipient'},{key:'message',label:'Message'},{key:'createdAt',label:'Date',render:r=>r.createdAt?new Date(r.createdAt).toLocaleString():'—'}]};return <div className="card"><h2 className="text-2xl font-bold mb-5 capitalize">{type}</h2><DataTable columns={configs[type]} data={rows} loading={loading}/><Pagination page={page} totalPages={pages} onPageChange={setPage}/></div>}
